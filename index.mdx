---
title: "Mat Dynamic Form"
mode: "custom"
---

<div className="relative overflow-hidden dark:bg-[#0f1117] bg-white py-20">
  <div className="max-w-7xl mx-auto px-6 lg:px-8">
    <div className="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
      <div className="lg:col-span-7">
        <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold dark:text-white text-gray-900 mb-6">
          Build Dynamic Forms with JSON Configuration
        </h1>
        <p className="text-lg sm:text-xl dark:text-gray-400 text-gray-600 mb-8 max-w-2xl">
          Mat Dynamic Form is an Angular library that transforms JSON objects into fully functional, beautiful forms using Angular Material components. No more repetitive form code.
        </p>
        <div className="flex flex-col sm:flex-row gap-4 flex-wrap">
          <a 
            href="/quickstart" 
            className="inline-flex items-center justify-center px-6 py-3 rounded-lg font-semibold text-white transition-colors no-underline"
            style={{ backgroundColor: '#94cb04' }}
            onMouseOver={(e) => e.currentTarget.style.backgroundColor = '#7aa303'}
            onMouseOut={(e) => e.currentTarget.style.backgroundColor = '#94cb04'}
          >
            Get Started
            <svg className="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
          <a 
            href="/api/form-structure" 
            className="inline-flex items-center justify-center px-6 py-3 rounded-lg font-semibold dark:text-white text-gray-900 dark:bg-white/10 bg-gray-100 dark:border-white/30 border border-gray-300 dark:hover:bg-white/20 hover:bg-gray-200 transition-colors no-underline"
          >
            API Reference
          </a>
        </div>
      </div>
      <div className="lg:col-span-5 hidden lg:block">
        <div className="dark:bg-[#1a1d27] bg-gray-50 rounded-2xl p-6 dark:border-[#27272a] border border-gray-200">
          <pre className="text-sm overflow-x-auto"><code className="language-typescript">{`import { FormStructure, Input, Button } from 'mat-dynamic-form';

const formStructure = new FormStructure();
formStructure.title = 'User Profile';
formStructure.nodes = [
  new Input('name', 'Full Name'),
  new Input('email', 'Email Address')
];
formStructure.validateActions = [
  new Button('save', 'Save', {
    onEvent: (param) => console.log(param.structure.getValue())
  })
];`}</code></pre>
        </div>
      </div>
    </div>
  </div>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <div className="text-center mb-12">
    <h2 className="text-3xl font-bold dark:text-white text-gray-900 mb-4">Quick Start</h2>
    <p className="text-lg dark:text-gray-400 text-gray-600">Get your first dynamic form running in three simple steps</p>
  </div>

  <Steps>
    <Step title="Install the package">
      Install Mat Dynamic Form and its peer dependency Angular Material:
      
      <CodeGroup>
      ```bash npm
      npm install mat-dynamic-form @angular/material @angular/cdk
      ```
      
      ```bash yarn
      yarn add mat-dynamic-form @angular/material @angular/cdk
      ```
      
      ```bash pnpm
      pnpm add mat-dynamic-form @angular/material @angular/cdk
      ```
      </CodeGroup>
    </Step>

    <Step title="Import the module">
      Add `MatDynamicFormModule` to your Angular module:
      
      ```typescript app.module.ts
      import { NgModule } from '@angular/core';
      import { MatDynamicFormModule } from 'mat-dynamic-form';

      @NgModule({
        imports: [
          MatDynamicFormModule
        ]
      })
      export class AppModule { }
      ```
    </Step>

    <Step title="Create your first form">
      Define your form structure and add the component to your template:
      
      ```typescript component.ts
      import { Component } from '@angular/core';
      import { FormStructure, Input, Button } from 'mat-dynamic-form';

      @Component({
        selector: 'app-demo',
        template: '<mat-dynamic-form [structure]="formStructure"></mat-dynamic-form>'
      })
      export class DemoComponent {
        formStructure = new FormStructure();

        constructor() {
          this.formStructure.title = 'Contact Form';
          this.formStructure.nodes = [
            new Input('name', 'Name'),
            new Input('email', 'Email')
          ];
          this.formStructure.validateActions = [
            new Button('submit', 'Submit', {
              onEvent: (e) => console.log(e.structure.getValue())
            })
          ];
        }
      }
      ```
      
      <Note>Your form is now ready! The library handles all form validation, state management, and Angular Material styling automatically.</Note>
    </Step>
  </Steps>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <div className="text-center mb-12">
    <h2 className="text-3xl font-bold dark:text-white text-gray-900 mb-4">Explore Components</h2>
    <p className="text-lg dark:text-gray-400 text-gray-600">Discover the full range of form components available</p>
  </div>

  <CardGroup cols={3}>
    <Card title="Input Fields" icon="keyboard" href="/components/input">
      Text, password, number, and textarea inputs with validation
    </Card>
    <Card title="Dropdowns & Selects" icon="list" href="/components/dropdown">
      Single and multi-select dropdowns with autocomplete support
    </Card>
    <Card title="Date & Time" icon="calendar" href="/components/date-picker">
      Date pickers, time pickers, and date range selectors
    </Card>
    <Card title="File Upload" icon="file-arrow-up" href="/components/input-file">
      Drag-and-drop file uploads with validation
    </Card>
    <Card title="Checkboxes & Radios" icon="circle-check" href="/components/checkbox">
      Checkbox and radio button groups
    </Card>
    <Card title="Custom Components" icon="puzzle-piece" href="/components/custom-component">
      Integrate your own Angular components
    </Card>
  </CardGroup>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <div className="text-center mb-12">
    <h2 className="text-3xl font-bold dark:text-white text-gray-900 mb-4">Key Features</h2>
    <p className="text-lg dark:text-gray-400 text-gray-600">Everything you need to build powerful forms</p>
  </div>

  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
    <div className="dark:bg-[#1a1d27] bg-gray-50 rounded-xl p-6 dark:border-[#27272a] border border-gray-200">
      <div className="flex items-start gap-4">
        <div className="flex-shrink-0 w-10 h-10 rounded-lg flex items-center justify-center" style={{ backgroundColor: '#94cb04' }}>
          <svg className="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
        </div>
        <div>
          <h3 className="text-lg font-semibold dark:text-white text-gray-900 mb-2">JSON-Driven Configuration</h3>
          <p className="dark:text-gray-400 text-gray-600">Define complex forms with simple JSON objects. No repetitive template code.</p>
        </div>
      </div>
    </div>

    <div className="dark:bg-[#1a1d27] bg-gray-50 rounded-xl p-6 dark:border-[#27272a] border border-gray-200">
      <div className="flex items-start gap-4">
        <div className="flex-shrink-0 w-10 h-10 rounded-lg flex items-center justify-center" style={{ backgroundColor: '#94cb04' }}>
          <svg className="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <div>
          <h3 className="text-lg font-semibold dark:text-white text-gray-900 mb-2">Built-in Validation</h3>
          <p className="dark:text-gray-400 text-gray-600">Leverage Angular's reactive forms validators with global and field-level rules.</p>
        </div>
      </div>
    </div>

    <div className="dark:bg-[#1a1d27] bg-gray-50 rounded-xl p-6 dark:border-[#27272a] border border-gray-200">
      <div className="flex items-start gap-4">
        <div className="flex-shrink-0 w-10 h-10 rounded-lg flex items-center justify-center" style={{ backgroundColor: '#94cb04' }}>
          <svg className="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01" />
          </svg>
        </div>
        <div>
          <h3 className="text-lg font-semibold dark:text-white text-gray-900 mb-2">Material Design</h3>
          <p className="dark:text-gray-400 text-gray-600">Beautiful, accessible forms using Angular Material components out of the box.</p>
        </div>
      </div>
    </div>

    <div className="dark:bg-[#1a1d27] bg-gray-50 rounded-xl p-6 dark:border-[#27272a] border border-gray-200">
      <div className="flex items-start gap-4">
        <div className="flex-shrink-0 w-10 h-10 rounded-lg flex items-center justify-center" style={{ backgroundColor: '#94cb04' }}>
          <svg className="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
          </svg>
        </div>
        <div>
          <h3 className="text-lg font-semibold dark:text-white text-gray-900 mb-2">Dynamic Node Management</h3>
          <p className="dark:text-gray-400 text-gray-600">Add, remove, or modify form fields at runtime based on user interactions.</p>
        </div>
      </div>
    </div>
  </div>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <div className="text-center mb-12">
    <h2 className="text-3xl font-bold dark:text-white text-gray-900 mb-4">Learn More</h2>
    <p className="text-lg dark:text-gray-400 text-gray-600">Dive deeper into Mat Dynamic Form</p>
  </div>

  <CardGroup cols={2}>
    <Card title="Core Concepts" icon="lightbulb" href="/concepts/form-structure">
      Understand FormStructure, Nodes, Actions, and Validators
    </Card>
    <Card title="Advanced Features" icon="wand-magic-sparkles" href="/advanced/dynamic-nodes">
      Explore dynamic forms, custom validators, and event handling
    </Card>
    <Card title="Real-World Examples" icon="code" href="/examples/sign-up-form">
      See complete examples like sign-up forms and conditional fields
    </Card>
    <Card title="API Reference" icon="book" href="/api/form-structure">
      Complete API documentation for all classes and methods
    </Card>
  </CardGroup>
</div>

<div className="mt-20 mb-16 max-w-5xl mx-auto px-6">
  <div className="dark:bg-[#1a1d27] bg-gray-50 rounded-2xl p-8 lg:p-12 dark:border-[#27272a] border border-gray-200 text-center">
    <h2 className="text-2xl lg:text-3xl font-bold dark:text-white text-gray-900 mb-4">Ready to build your first dynamic form?</h2>
    <p className="text-lg dark:text-gray-400 text-gray-600 mb-8 max-w-2xl mx-auto">
      Follow our quickstart guide and create a fully functional form in minutes.
    </p>
    <a 
      href="/quickstart" 
      className="inline-flex items-center justify-center px-8 py-3 rounded-lg font-semibold text-white transition-colors no-underline"
      style={{ backgroundColor: '#94cb04' }}
      onMouseOver={(e) => e.currentTarget.style.backgroundColor = '#7aa303'}
      onMouseOut={(e) => e.currentTarget.style.backgroundColor = '#94cb04'}
    >
      Get Started Now
      <svg className="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 7l5 5m0 0l-5 5m5-5H6" />
      </svg>
    </a>
  </div>
</div>
